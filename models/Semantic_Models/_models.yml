version: 2

models:
  - name: time_spine_daily
    description: A time spine with one row per day this is used to calculate all dates regardless of data absence
    time_spine:
      standard_granularity_column: date_day

    columns:
      - name: date_day
        description: Base date column
        granularity: day
